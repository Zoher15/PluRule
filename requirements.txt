# Core dependencies for Reddit mod collection pipeline
zstandard>=0.21.0
orjson>=3.9.0

# Fallback JSON libraries
ujson>=5.8.0

# Data processing
numpy>=1.24.0
pandas>=2.0.0
scipy>=1.10.0

# Machine learning (for rule matching stage)
torch>=2.0.0
transformers>=4.30.0
vllm>=0.2.0

# Reddit API (for subreddit data collection)
praw>=7.7.0

# HTML processing (for rule text cleaning)
beautifulsoup4>=4.12.0
html5lib>=1.1

# Progress tracking
tqdm>=4.65.0

# Development/testing (optional)
pytest>=7.4.0